import { SxProps } from '@mui/material';

export const buttons: SxProps = {
  mt: 2,
  justifyContent: 'flex-end',
  gap: 2,
};

export const textArea: SxProps = {
  mt: 2,
  width: '100%',
};

export const alert: SxProps = {
  mt: 2,
};
